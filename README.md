# Animated Portfolio (Generated)

This is a ready-to-run Vite + React + Tailwind project with animations.

## Quick start

1. Install dependencies:
   npm install

2. Run dev server:
   npm run dev

## Notes
- Replace images in src/assets with your real photos.
- Ensure Node.js 18+ is installed.
